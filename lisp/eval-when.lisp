(eval-when (:compile-toplevel)
    (print "Hello from compile!")
      (terpri))
(eval-when (:load-toplevel)
    (print "Hello from load!")
      (terpri))
(eval-when (:execute)
    (print "Hello from execute!")
      (terpri))

(defun foo ()
  (eval-when (:compile-toplevel)
             (print "Compile says foo!")
             (terpri))
  (eval-when (:load-toplevel)
             (print "Load says foo!")
             (terpri))
  (eval-when (:execute)
             (print "Execute says foo!")
             (terpri)))
